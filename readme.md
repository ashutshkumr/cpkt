# CPKT - Minimal Network Packet Serializer

### Usage

1. Get source

    ```sh
    git clone https://github.com/ashutshkumr/cpkt.git && cpkt
    # run unit tests
    make test
    ```

2. Serialize packet (TBD)

    ```sh
    ```
